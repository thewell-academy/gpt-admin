import 'package:flutter/cupertino.dart';

class CreateQuestionPdfPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "문제 PDF 만들기",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}